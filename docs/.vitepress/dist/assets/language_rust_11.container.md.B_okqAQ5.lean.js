import{_ as a,c as n,a0 as e,o as p}from"./chunks/framework.DDvRCNFD.js";const u=JSON.parse('{"title":"container","description":"","frontmatter":{},"headers":[],"relativePath":"language/rust/11.container.md","filePath":"language/rust/11.container.md"}'),t={name:"language/rust/11.container.md"};function l(o,s,i,c,d,r){return p(),n("div",null,s[0]||(s[0]=[e(`<h1 id="container" tabindex="-1">container <a class="header-anchor" href="#container" aria-label="Permalink to &quot;container&quot;">​</a></h1><p>Rust 中，容器是存储和组织多个值的数据结构。Rust 提供了多种容器类型，每种容器都有其特定的用途和特点。以下是 Rust 中常用的容器：</p><ol><li><strong>Vector (<code>Vec&lt;T&gt;</code>)</strong><ul><li>是一个可增长的数组，用于存储同一类型的元素。</li><li>有助于动态地添加或删除元素。</li></ul></li><li><strong>String (<code>String</code>)</strong><ul><li>是一个 UTF-8 编码的字符串类型，可以动态增长。</li><li>不仅仅是一个字符数组，因为 UTF-8 编码的字符可以占用多个字节。</li></ul></li><li><strong>Hash Map (<code>HashMap&lt;K, V&gt;</code>)</strong><ul><li>是一个基于键的值存储系统。</li><li>允许你存储值并使用唯一的键来快速检索它们。</li></ul></li><li><strong>HashSet (<code>HashSet&lt;T&gt;</code>)</strong><ul><li>是一个集合，其中每个元素都是唯一的。</li><li>有助于快速检查一个值是否存在于集合中，因为它使用哈希进行存储。</li></ul></li><li><strong>LinkedList (<code>LinkedList&lt;T&gt;</code>)</strong><ul><li>是一个双向链表。</li><li>允许快速的前后插入，但随机访问较慢。</li></ul></li><li><strong>BinaryHeap (<code>BinaryHeap&lt;T&gt;</code>)</strong><ul><li>是一个优先队列。</li><li>允许你保持元素的排序，以便始终能够快速访问最大或最小的元素。</li></ul></li></ol><p>每种容器都有其独特的性质和使用场景。选择哪种容器取决于你的具体需求，例如，你需要快速随机访问、快速插入、键值对存储等。</p><p><strong>Vec&lt; T&gt;</strong></p><p><code>Vec&lt;T&gt;</code> 是 Rust 中的一个动态数组（或称为向量），它可以在运行时增长或缩小。<code>Vec&lt;T&gt;</code> 是一个泛型容器，其中 <code>T</code> 表示它将存储的元素的类型。以下是关于 <code>Vec&lt;T&gt;</code> 的一些基本信息和使用示例：</p><p><strong>创建：</strong></p><ol><li><p><strong>使用 <code>vec!</code> 宏创建向量</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let v = vec![1, 2, 3, 4, 5];</span></span></code></pre></div></li><li><p><strong>使用 <code>Vec::new()</code> 方法创建一个空向量</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut v: Vec&lt;i32&gt; = Vec::new();</span></span></code></pre></div></li></ol><p><strong>添加元素：</strong></p><ul><li><p>使用 <code>push</code> 方法向向量的尾部添加元素</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut v = vec![1, 2];</span></span>
<span class="line"><span>v.push(3);</span></span></code></pre></div></li></ul><p><strong>访问元素：</strong></p><ol><li><p><strong>使用索引直接访问</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let v = vec![1, 2, 3];</span></span>
<span class="line"><span>let third = v[2];  // third 的值为 3</span></span></code></pre></div></li><li><p><strong>使用 <code>get</code> 方法安全地访问元素（返回 <code>Option&lt;&amp;T&gt;</code>）</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let v = vec![1, 2, 3];</span></span>
<span class="line"><span>let third = v.get(2);  // third 的值为 Some(&amp;3)</span></span></code></pre></div></li></ol><p><strong>遍历元素：</strong></p><ul><li><p>使用 <code>for</code> 循环遍历向量中的所有元素</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let v = vec![1, 2, 3];</span></span>
<span class="line"><span>for item in &amp;v {</span></span>
<span class="line"><span>    println!(&quot;{}&quot;, item);</span></span>
<span class="line"><span>}</span></span></code></pre></div></li></ul><p><strong>移除元素：</strong></p><ul><li><p>使用 <code>pop</code> 方法从向量的尾部移除元素</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut v = vec![1, 2, 3];</span></span>
<span class="line"><span>v.pop();  // v 现在为 [1, 2]</span></span></code></pre></div></li></ul><p><strong>大小和容量：</strong></p><ul><li>使用 <code>len</code> 方法获取向量的长度。</li><li>使用 <code>capacity</code> 方法获取向量的容量。</li><li>使用 <code>shrink_to_fit</code> 方法将向量的容量减少到与其长度相同。</li></ul><p><strong>其他特点：</strong></p><ol><li><strong>自动增长</strong>：当向量的大小超过其容量时，它会自动分配新的内存并将旧元素移至新位置。</li><li><strong>连续内存</strong>：向量的元素在内存中是连续存储的，这使得它对于随机访问非常快。</li><li><strong>拥有其数据</strong>：当 <code>Vec&lt;T&gt;</code> 被丢弃时，它的所有元素也都会被丢弃。</li></ol><p><strong>VecDeque&lt; T&gt;</strong></p><p>VecDeque&lt; T&gt; 是 Rust 中的双端队列 (double-ended queue)，它允许在队列的两端高效地添加和移除元素。这与 Vec&lt; T&gt; 不同，Vec&lt; T&gt; 优化了从尾部添加和移除元素，但从头部进行操作可能会非常慢。而 VecDeque&lt; T&gt; 则提供了均衡的性能。</p><p>以下是 VecDeque&lt; T&gt; 的一些基本用法和特性：</p><p><strong>创建</strong>使用 VecDeque::new() 创建一个空的 VecDeque&lt;T&gt;：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut deque: VecDeque&lt;i32&gt; = VecDeque::new();</span></span></code></pre></div><p><strong>添加元素</strong> 使用 push_back 在队列尾部添加元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deque.push_back(1);</span></span></code></pre></div><p>使用 push_front 在队列头部添加元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deque.push_front(0);</span></span></code></pre></div><p><strong>移除元素</strong> 使用 pop_back 从队列尾部移除元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deque.pop_back();  // 返回 Some(1)</span></span></code></pre></div><p><strong>使用 pop_front 从队列头部移除元素：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>deque.pop_front();  // 返回 Some(0)</span></span></code></pre></div><p><strong>其他操作</strong> front 和 back 可以获取队列的首元素和尾元素，但不移除它们。 len 返回队列中的元素数量。 is_empty 检查队列是否为空。 clear 清空队列的所有元素。</p><p>特性 连续内存：VecDeque&lt;T&gt; 内部使用一个环形缓冲区。这意味着物理内存可能是连续的，但逻辑上它可以被视为两部分。 自动增长：当 VecDeque&lt;T&gt; 的容量不足以容纳新元素时，它会自动增长。 VecDeque&lt;T&gt; 在需要从头部和尾部都进行添加和移除操作时是非常有用的，比如在实现某些算法（如 BFS 广度优先搜索）或数据结构（如滑动窗口）时。</p><p><strong>LinkedLisk&lt; T&gt;</strong></p><p>在 Rust 的标准库中，没有直接名为 <code>LinkedList&lt;T&gt;</code> 的数据结构。但是，有一个名为 <code>std::collections::LinkedList&lt;T&gt;</code> 的双向链表实现。这是一个基于节点的双向链表，允许 O(1) 的插入和删除操作。</p><p>下面是 <code>std::collections::LinkedList&lt;T&gt;</code> 的一些基本用法：</p><p><strong>创建</strong></p><ul><li><p>使用 <code>LinkedList::new()</code>创建一个空的 <code>LinkedList&lt;T&gt;</code></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::LinkedList;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut list: LinkedList&lt;i32&gt; = LinkedList::new();</span></span></code></pre></div></li></ul><p><strong>添加元素</strong></p><ul><li><p>使用 <code>push_back</code> 在链表尾部添加元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>list.push_back(1);</span></span></code></pre></div></li><li><p>使用 <code>push_front</code> 在链表头部添加元素</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>list.push_front(0);</span></span></code></pre></div></li></ul><p><strong>移除元素</strong></p><ul><li><p>使用 <code>pop_back</code> 从链表尾部移除元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rustCopy code</span></span>
<span class="line"><span>list.pop_back();  // 返回 Some(1)</span></span></code></pre></div></li><li><p>使用 <code>pop_front</code> 从链表头部移除元素：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rustCopy code</span></span>
<span class="line"><span>list.pop_front();  // 返回 Some(0)</span></span></code></pre></div></li></ul><p><strong>其他操作</strong></p><ul><li><code>front</code> 和 <code>back</code> 可以获取链表的首元素和尾元素，但不移除它们。</li><li><code>len</code> 返回链表中的元素数量。</li><li><code>is_empty</code> 检查链表是否为空。</li><li><code>clear</code> 清空链表的所有元素。</li></ul><p><strong>特性</strong></p><ul><li><strong>节点基础</strong>：<code>LinkedList&lt;T&gt;</code> 是基于节点的，每个节点都在堆上分配。</li><li><strong>双向</strong>：它是一个双向链表，这意味着每个节点都有一个到前一个和后一个节点的引用。</li></ul><p><strong>HashMap&lt;K,V&gt;/BTreeMap&lt;K,V&gt;</strong></p><p><code>HashMap&lt;K, V&gt;</code> 和 <code>BTreeMap&lt;K, V&gt;</code> 都是 Rust 中的关联数组（或称为字典）实现，它们允许用户存储键值对，并根据键快速查找值。但它们的内部实现和特性有所不同。</p><p><strong><code>HashMap&lt;K, V&gt;</code></strong></p><p><strong>特点</strong>:</p><ul><li>基于哈希表实现。</li><li>插入、删除和查找操作通常是 O(1)。</li><li>键的顺序是不确定的，每次迭代可能都会变化。</li></ul><p><strong>用法</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut scores = HashMap::new();</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>scores.insert(&quot;Blue&quot;, 10);</span></span>
<span class="line"><span>scores.insert(&quot;Red&quot;, 50);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let team_name = String::from(&quot;Blue&quot;);</span></span>
<span class="line"><span>let score = scores.get(&amp;team_name);</span></span></code></pre></div><p><strong><code>BTreeMap&lt;K, V&gt;</code></strong></p><p><strong>特点</strong>:</p><ul><li>基于平衡二叉搜索树（B-Tree）实现。</li><li>插入、删除和查找操作都是 O(log n)。</li><li>键是有序的，这意味着你可以对它们进行排序的迭代。</li></ul><p><strong>用法</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::BTreeMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut map = BTreeMap::new();</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>map.insert(3, &quot;c&quot;);</span></span>
<span class="line"><span>map.insert(2, &quot;b&quot;);</span></span>
<span class="line"><span>map.insert(1, &quot;a&quot;);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>for (key, value) in &amp;map {</span></span>
<span class="line"><span>    println!(&quot;{}: {}&quot;, key, value);</span></span>
<span class="line"><span>}</span></span></code></pre></div><p><strong>何时选择哪一个？</strong></p><ul><li><strong>性能</strong>: 对于大多数用途，<code>HashMap</code> 由于其常数时间的操作通常更快。</li><li><strong>键的顺序</strong>: 如果你需要有序的键，例如范围查询或顺序迭代，那么 <code>BTreeMap</code> 是一个好选择。</li><li><strong>确定性</strong>: <code>HashMap</code> 默认使用一个随机的哈希种子，这意味着它的迭代顺序在每次运行时都可能不同。如果你需要更加确定性的行为（例如，为了测试），<code>BTreeMap</code> 提供了一个固定的顺序。</li></ul><p>总之，选择 <code>HashMap</code> 还是 <code>BTreeMap</code> 取决于你的具体需求和所面临的性能考虑。在许多情况下，<code>HashMap</code> 都是一个很好的默认选择，但如果你需要有序或确定性的行为，那么 <code>BTreeMap</code> 可能更合适。</p><p><strong>创建新的哈希表</strong></p><p>问：什么是哈希表？</p><p>是一种存储键值对的数据结构，它支持近似常数时间的插入、删除和查找操作。哈希表的工作原理基于一个简单的原则：使用一个哈希函数将键转换为一个数组索引，然后在该索引处存储相应的值。</p><p>以下是哈希表的一些关键特点和原理：</p><ol><li><strong>哈希函数</strong>：哈希函数负责将键转换为数组索引。理想情况下，哈希函数应该均匀地分布键，以避免太多的键映射到同一索引（这称为哈希冲突）。</li><li><strong>冲突解决</strong>：当两个或多个键的哈希值相同时，会发生哈希冲突。常见的冲突解决策略有开放寻址和链地址法。 <ul><li><strong>开放寻址</strong>：当冲突发生时，寻找下一个空闲的槽位存储键值对。</li><li><strong>链地址法</strong>：每个槽位包含一个链表或另一种数据结构，用于存储所有哈希到该槽位的键值对。</li></ul></li><li><strong>动态调整大小</strong>：为了维持操作的高效性，哈希表可能需要根据其大小和填充因子（即存储的键值对数与总槽数的比例）动态调整其大小。</li><li><strong>性能</strong>：在没有冲突的情况下，哈希表的插入、删除和查找操作通常是常数时间的。但是，当冲突增加时，性能可能下降。</li><li><strong>应用</strong>：哈希表在很多应用中都非常有用，例如数据库、缓存、查找表等。</li></ol><p>在 Rust 中，哈希表由 <code>std::collections::HashMap</code> 类型表示。它提供了创建、修改和查询哈希表的方法。例如，你可以使用 <code>insert</code> 方法添加元素，使用 <code>get</code> 方法根据键查找值，或使用 <code>remove</code> 方法删除键值对。</p><p>在 Rust 中，你可以使用 <code>HashMap</code> 来创建一个哈希表。以下是如何创建新的哈希表的几种方法：</p><p><strong>使用 <code>new</code> 方法</strong></p><p>创建一个空的 <code>HashMap</code>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut map = HashMap::new();</span></span></code></pre></div><p><strong>使用 <code>insert</code> 方法添加键值对</strong></p><p>创建了一个空的 <code>HashMap</code> 后，你可以使用 <code>insert</code> 方法向其添加键值对：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>map.insert(&quot;name&quot;, &quot;Alice&quot;);</span></span>
<span class="line"><span>map.insert(&quot;age&quot;, &quot;30&quot;);</span></span></code></pre></div><p><strong>使用 <code>collect</code> 方法从迭代器创建</strong></p><p>你可以使用一个迭代器（例如一个由元组组成的数组或向量的迭代器）和 <code>collect</code> 方法来创建一个 <code>HashMap</code>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let data = [(&quot;name&quot;, &quot;Alice&quot;), (&quot;age&quot;, &quot;30&quot;)];</span></span>
<span class="line"><span>let map: HashMap&lt;_, _&gt; = data.iter().cloned().collect();</span></span></code></pre></div><p><strong>使用 <code>HashMap::with_capacity</code></strong></p><p>如果你预先知道哈希表的大小，可以使用 <code>with_capacity</code> 方法为其预分配空间，这可以避免随后的重新分配：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut map = HashMap::with_capacity(10);</span></span>
<span class="line"><span>map.insert(&quot;name&quot;, &quot;Alice&quot;);</span></span></code></pre></div><p>一旦你有了一个 <code>HashMap</code>，你就可以使用其提供的各种方法来插入、删除或查找键值对，以及执行其他操作。</p><p><strong>访问哈希表的元素</strong></p><ol><li><p><strong>使用 <code>get</code> 方法</strong>:</p><ul><li><code>get</code> 方法用于根据给定的键查找值。</li><li>它返回一个 <code>Option</code>，其中 <code>Some(&amp;value)</code> 表示找到了值，<code>None</code> 表示未找到。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut scores = HashMap::new();</span></span>
<span class="line"><span>scores.insert(String::from(&quot;Blue&quot;), 10);</span></span>
<span class="line"><span>scores.insert(String::from(&quot;Yellow&quot;), 50);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let team_name = String::from(&quot;Blue&quot;);</span></span>
<span class="line"><span>let score = scores.get(&amp;team_name);</span></span>
<span class="line"><span>match score {</span></span>
<span class="line"><span>    Some(&amp;n) =&gt; println!(&quot;Score for {}: {}&quot;, team_name, n),</span></span>
<span class="line"><span>    None =&gt; println!(&quot;No score for team {}&quot;, team_name),</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p><strong>直接使用键访问</strong>:</p><ul><li>使用<code>[key]</code>语法直接访问元素。但这在键不存在时会引发恐慌。</li><li>使用<code>entry(key)</code>与<code>or_insert(value)</code>结合可以更安全地访问和插入。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let mut count = HashMap::new();</span></span>
<span class="line"><span>count.entry(String::from(&quot;Blue&quot;)).or_insert(10);</span></span></code></pre></div></li><li><p><strong>遍历HashMap</strong>:</p><ul><li>使用<code>for</code>循环与<code>iter</code>方法结合可以遍历哈希表的所有键值对。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for (key, value) in &amp;scores {</span></span>
<span class="line"><span>    println!(&quot;{}: {}&quot;, key, value);</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p><strong>更新值</strong>:</p><ul><li>可以使用<code>insert</code>方法覆盖旧值。</li><li>使用<code>entry(key).or_insert(value)</code>结合可以只在键不存在时插入。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>*scores.entry(String::from(&quot;Blue&quot;)).or_insert(0) += 10;</span></span></code></pre></div></li><li><p><strong>删除元素</strong>:</p><ul><li>使用<code>remove</code>方法删除键和其关联的值。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scores.remove(&amp;String::from(&quot;Blue&quot;));</span></span></code></pre></div></li></ol><p><strong>迭代哈希表</strong></p><p>在 Rust 中，可以使用 <code>for</code> 循环遍历 <code>HashMap</code>（哈希表）的内容。以下是如何迭代哈希表的基本方法：</p><ol><li><p><strong>遍历所有的键值对</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut map = HashMap::new();</span></span>
<span class="line"><span>map.insert(&quot;key1&quot;, &quot;value1&quot;);</span></span>
<span class="line"><span>map.insert(&quot;key2&quot;, &quot;value2&quot;);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>for (key, value) in &amp;map {</span></span>
<span class="line"><span>    println!(&quot;Key: {}, Value: {}&quot;, key, value);</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p><strong>只遍历所有的键</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for key in map.keys() {</span></span>
<span class="line"><span>    println!(&quot;Key: {}&quot;, key);</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p><strong>只遍历所有的值</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for value in map.values() {</span></span>
<span class="line"><span>    println!(&quot;Value: {}&quot;, value);</span></span>
<span class="line"><span>}</span></span></code></pre></div></li><li><p><strong>可变引用遍历</strong>: 如果你想在迭代过程中修改哈希表的值，可以使用可变引用来遍历：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for value in map.values_mut() {</span></span>
<span class="line"><span>    // 这里可以修改 value</span></span>
<span class="line"><span>}</span></span></code></pre></div></li></ol><p>注意：由于 <code>HashMap</code> 是基于哈希的，所以遍历的顺序并不保证与插入的顺序相同。</p><p><strong>哈希表和所有权</strong></p><p>在 Rust 中，哈希表（<code>HashMap</code>）和所有权的概念紧密相连。以下是一些与 <code>HashMap</code> 和所有权相关的重要点：</p><ol><li><p><strong>所有权转移</strong>: 当将值插入到 <code>HashMap</code> 中时，这些值的所有权会被转移给哈希表。这意味着原始值将不再可用。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashMap;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let key = String::from(&quot;name&quot;);</span></span>
<span class="line"><span>let value = String::from(&quot;Alice&quot;);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut map = HashMap::new();</span></span>
<span class="line"><span>map.insert(key, value);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>// 此时 key 和 value 都不再可用，因为它们的所有权已经转移到了 map 中。</span></span></code></pre></div></li><li><p><strong>引用作为键或值</strong>: 可以使用引用作为键或值，但需要确保这些引用在哈希表的整个生命周期中都有效。这通常涉及到生命周期注解。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>let key = String::from(&quot;name&quot;);</span></span>
<span class="line"><span>let value = String::from(&quot;Alice&quot;);</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut map: HashMap&lt;&amp;str, &amp;str&gt; = HashMap::new();</span></span>
<span class="line"><span>map.insert(&amp;key, &amp;value);</span></span></code></pre></div></li><li><p><strong>获取值</strong>: 使用 <code>get</code> 方法从 <code>HashMap</code> 获取值时，获得的是一个引用。如果需要拥有值的所有权，需要使用 <code>remove</code> 方法。</p></li><li><p><strong>更新值</strong>: 更新 <code>HashMap</code> 中的值不需要取出值，修改它然后再放回去。你可以直接使用如 <code>entry</code> 和 <code>or_insert</code> 这样的方法。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rustCopy code</span></span>
<span class="line"><span>*map.entry(String::from(&quot;name&quot;)).or_insert(String::from(&quot;Bob&quot;)) += &quot; Smith&quot;;</span></span></code></pre></div></li><li><p><strong>删除值</strong>: 使用 <code>remove</code> 方法从 <code>HashMap</code> 中删除键值对会将值返回给你，这样你就获得了这个值的所有权。</p></li></ol><p><strong>HashSet&lt; T&gt;/BTreeSet&lt; T&gt;</strong></p><p><code>HashSet&lt;T&gt;</code> 和 <code>BTreeSet&lt;T&gt;</code> 都是 Rust 中的集合（set）数据结构，它们提供了一组不重复的元素集。这两种集合在某些方面类似，但它们的内部实现和某些特性是不同的。</p><ol><li><p><strong>HashSet&lt; T&gt;</strong>:</p><ul><li>基于哈希表实现。</li><li>插入、删除和查找的平均时间复杂度为 O(1)。</li><li>不保证元素的顺序。</li><li>快速的成员检查。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>use std::collections::HashSet;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut fruits = HashSet::new();</span></span>
<span class="line"><span>fruits.insert(&quot;apple&quot;);</span></span>
<span class="line"><span>fruits.insert(&quot;banana&quot;);</span></span>
<span class="line"><span>fruits.insert(&quot;orange&quot;);</span></span></code></pre></div></li><li><p><strong>BTreeSet&lt;T&gt;</strong>:</p><ul><li>基于平衡二叉树实现。</li><li>插入、删除和查找的时间复杂度为 O(log n)。</li><li>元素总是有序的。</li><li>支持有序集合的操作，如找到最小或最大的元素。</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rustCopy codeuse std::collections::BTreeSet;</span></span>
<span class="line"><span>​</span></span>
<span class="line"><span>let mut numbers = BTreeSet::new();</span></span>
<span class="line"><span>numbers.insert(3);</span></span>
<span class="line"><span>numbers.insert(1);</span></span>
<span class="line"><span>numbers.insert(4);</span></span></code></pre></div></li></ol><p><strong>比较</strong>:</p><ul><li><strong>性能</strong>: 对于大多数使用情况，<code>HashSet</code> 的性能可能更好，因为它提供了近似常数时间的操作。但是，如果需要有序的集合或范围查询，<code>BTreeSet</code> 可能是更好的选择。</li><li><strong>功能</strong>: <code>BTreeSet</code> 提供了一些有序集合特有的功能，如 <code>range</code> 方法，它允许对一定范围内的元素进行迭代。</li><li><strong>内存</strong>: 由于 <code>BTreeSet</code> 的树形结构，它可能会使用比 <code>HashSet</code> 更多的内存。</li></ul><p>\\</p>`,98)]))}const h=a(t,[["render",l]]);export{u as __pageData,h as default};
